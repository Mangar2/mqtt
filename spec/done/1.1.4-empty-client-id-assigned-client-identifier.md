# Work Order: Implement MQTT 5.0 CONNECT 1.1.4 (Empty Client ID)

## Background
Integration test `connect/basic/empty_client_id_assigned` is currently failing.
Observed result: CONNACK reason code `0x85` instead of success.
Expected behavior from test plan section 1.1.4:
- Client connects with empty Client ID
- Broker accepts connection
- Broker assigns a new Client ID
- CONNACK includes `Assigned Client Identifier` property

## Reproduction
Run:

```sh
python3 test/run_integration_tests.py --filter connect/basic/empty_client_id_assigned
```

Current failure message:
- reason code mismatch: expected `0x00`, got `0x85`

## Required Implementation
Implement broker behavior for MQTT 5.0 empty Client ID handling:
1. Accept CONNECT packets with empty Client ID when protocol rules allow it.
2. Generate a broker-assigned unique Client ID.
3. Attach the generated ID to CONNACK as `Assigned Client Identifier`.
4. Complete connection establishment with CONNACK success (`0x00`).
5. Ensure the assigned ID is used as the effective session/client identity.

## Acceptance Criteria
1. Test `connect/basic/empty_client_id_assigned` passes.
2. Full suite `python3 test/run_integration_tests.py --filter connect/basic` passes.
3. No regressions for explicit Client ID handling (`connect/basic/explicit_client_id_used`).
4. Behavior matches integration test plan item 1.1.4.

## Notes
- This is an implementation gap, not a test bug.
- Keep implementation MQTT 5.0 compliant.
